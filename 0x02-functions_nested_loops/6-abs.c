#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @abs: The integer to be computed.
 * Return: 0 to main
 */
int _abs(int abs)
{

	if (abs >= 0)
		return (abs);
	else
		return (-abs);
}
